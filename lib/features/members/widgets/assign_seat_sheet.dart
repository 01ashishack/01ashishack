import 'package:flutter/material.dart';

class AssignSeatSheet extends StatelessWidget { const AssignSeatSheet({super.key}); @override Widget build(BuildContext context) => const Padding(padding: EdgeInsets.all(16), child: Text('Assign seat sheet')); }
