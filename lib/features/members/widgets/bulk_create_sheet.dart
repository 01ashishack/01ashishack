import 'package:flutter/material.dart';

class BulkCreateSheet extends StatelessWidget { const BulkCreateSheet({super.key}); @override Widget build(BuildContext context) => const Padding(padding: EdgeInsets.all(16), child: Text('Bulk create seats sheet')); }
