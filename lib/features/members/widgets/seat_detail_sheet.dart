import 'package:flutter/material.dart';

class SeatDetailSheet extends StatelessWidget { const SeatDetailSheet({super.key}); @override Widget build(BuildContext context) => const Padding(padding: EdgeInsets.all(16), child: Text('Seat detail sheet')); }
